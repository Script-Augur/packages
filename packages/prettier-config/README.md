# @script-augur/prettier-config

Shared Prettier settings. Includes `prettier-plugin-tailwindcss` only.

**Import sorting is not handled here** — use `@script-augur/eslint-config`.

## Usage

```json
{
  "prettier": "@script-augur/prettier-config"
}
```
